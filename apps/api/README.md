# Qlidex API

Express backend for the Qlidex monorepo.

## Endpoints

- `GET /health` - service status.
- `POST /api/contact` - accepts callback requests from the frontend.

The API reads configuration from the root `.env` file.
